package com.complyco.gradle.config

/**
 * The purpose of this object is to hold all of the common Android configuration fields
 * for all Gradle files.
 */
object AndroidConfig {

    const val SDK_COMPILE = 36

    const val SDK_MIN = 28

    const val SDK_TARGET = SDK_COMPILE
}